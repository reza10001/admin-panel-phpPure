<?php

// if ( ini_set( 'session.cookie_secure', '1' ) === false ); 
    // throw new \Exception( 'Unable to set display_errors.' );
 // if ( ini_set( 'session.cookie_samesite', "" ) === false ); 
    // throw new \Exception( 'Unable to set display_errors.' );
 // if ( ini_set( 'session.cookie_domain',"example.com" ) === false ); 
    // throw new \Exception( 'Unable to set display_errors.' );    
     
    
include "bootstrap/boot.php";

echo 'index';
// ...

